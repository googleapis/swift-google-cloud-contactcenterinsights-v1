// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Selector of all available annotators and phrase matchers to run.
public struct AnnotatorSelector: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Whether to run the interruption annotator.
  public var runInterruptionAnnotator: Swift.Bool = Swift.Bool()

  /// Whether to run the silence annotator.
  public var runSilenceAnnotator: Swift.Bool = Swift.Bool()

  /// Whether to run the active phrase matcher annotator(s).
  public var runPhraseMatcherAnnotator: Swift.Bool = Swift.Bool()

  /// The list of phrase matchers to run. If not provided, all active phrase
  /// matchers will be used. If inactive phrase matchers are provided, they will
  /// not be used. Phrase matchers will be run only if
  /// run_phrase_matcher_annotator is set to true. Format:
  /// projects/{project}/locations/{location}/phraseMatchers/{phrase_matcher}
  public var phraseMatchers: [Swift.String] = []

  /// Whether to run the sentiment annotator.
  public var runSentimentAnnotator: Swift.Bool = Swift.Bool()

  /// Whether to run the entity annotator.
  public var runEntityAnnotator: Swift.Bool = Swift.Bool()

  /// Whether to run the intent annotator.
  public var runIntentAnnotator: Swift.Bool = Swift.Bool()

  /// Whether to run the issue model annotator. A model should have already been
  /// deployed for this to take effect.
  public var runIssueModelAnnotator: Swift.Bool = Swift.Bool()

  /// The issue model to run. If not provided, the most recently deployed topic
  /// model will be used. The provided issue model will only be used for
  /// inference if the issue model is deployed and if run_issue_model_annotator
  /// is set to true. If more than one issue model is provided, only the first
  /// provided issue model will be used for inference.
  public var issueModels: [Swift.String] = []

  /// Whether to run the summarization annotator.
  public var runSummarizationAnnotator: Swift.Bool = Swift.Bool()

  /// Configuration for the summarization annotator.
  public var summarizationConfig: AnnotatorSelector.SummarizationConfig? = nil

  /// Whether to run the QA annotator.
  public var runQaAnnotator: Swift.Bool = Swift.Bool()

  /// Configuration for the QA annotator.
  public var qaConfig: AnnotatorSelector.QaConfig? = nil

  /// Initialize a new instance of `AnnotatorSelector`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = AnnotatorSelector().with { $0.runInterruptionAnnotator = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Configuration for summarization.
  public struct SummarizationConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Summarization must use either a preexisting conversation profile or one
    /// of the supported default models.
    public var modelSource: OneOf_ModelSource? = nil

    /// Initialize a new instance of `SummarizationConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SummarizationConfig().with { $0.conversationProfile = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case conversationProfile = "conversationProfile"
      case summarizationModel = "summarizationModel"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)

      var modelSource: OneOf_ModelSource? = nil
      let modelSourceCheckAndSet = {
        if modelSource != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'modelSource'"))
        }
        modelSource = $0
      }
      if let conversationProfile = try container.decodeIfPresent(
        Swift.String.self, forKey: .conversationProfile)
      {
        try modelSourceCheckAndSet(.conversationProfile(conversationProfile))
      }
      if let summarizationModel = try container.decodeIfPresent(
        AnnotatorSelector.SummarizationConfig.SummarizationModel.self, forKey: .summarizationModel)
      {
        try modelSourceCheckAndSet(.summarizationModel(summarizationModel))
      }
      self.modelSource = modelSource
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)

      if let choice = self.modelSource {
        switch choice {
        case .conversationProfile(let value):
          try container.encode(value, forKey: .conversationProfile)
        case .summarizationModel(let value):
          try container.encode(value, forKey: .summarizationModel)
        }
      }
    }

    /// Summarization model to use, if `conversation_profile` is not used.
    public enum SummarizationModel: Codable, Equatable, Sendable {
      /// Unspecified summarization model.
      case unspecified
      /// The CCAI baseline model.
      case baselineModel
      /// The CCAI baseline model, V2.0.
      case baselineModelV20
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .baselineModel: return 1
        case .baselineModelV20: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SUMMARIZATION_MODEL_UNSPECIFIED"
        case .baselineModel: return "BASELINE_MODEL"
        case .baselineModelV20: return "BASELINE_MODEL_V2_0"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SummarizationModel/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SUMMARIZATION_MODEL_UNSPECIFIED": self = .unspecified
        case "BASELINE_MODEL": self = .baselineModel
        case "BASELINE_MODEL_V2_0": self = .baselineModelV20
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SummarizationModel/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .baselineModel
        case 2: self = .baselineModelV20
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .baselineModel: return try container.encode(1)
        case .baselineModelV20: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Summarization must use either a preexisting conversation profile or one
    /// of the supported default models.
    public enum OneOf_ModelSource: Codable, Equatable, Sendable {
      /// Resource name of the Dialogflow conversation profile.
      /// Format:
      /// projects/{project}/locations/{location}/conversationProfiles/{conversation_profile}
      case conversationProfile(Swift.String)
      /// Default summarization model to be used.
      case summarizationModel(AnnotatorSelector.SummarizationConfig.SummarizationModel)
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.AnnotatorSelector.SummarizationConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration for the QA feature.
  public struct QaConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Which scorecards should be scored.
    public var scorecardSource: OneOf_ScorecardSource? = nil

    /// Initialize a new instance of `QaConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = QaConfig().with { $0.scorecardList = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case scorecardList = "scorecardList"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)

      var scorecardSource: OneOf_ScorecardSource? = nil
      let scorecardSourceCheckAndSet = {
        if scorecardSource != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'scorecardSource'"))
        }
        scorecardSource = $0
      }
      if let scorecardList = try container.decodeIfPresent(
        AnnotatorSelector.QaConfig.ScorecardList?.self, forKey: .scorecardList)
      {
        try scorecardSourceCheckAndSet(.scorecardList(scorecardList))
      }
      self.scorecardSource = scorecardSource
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)

      if let choice = self.scorecardSource {
        switch choice {
        case .scorecardList(let value):
          try container.encode(value, forKey: .scorecardList)
        }
      }
    }

    /// Container for a list of scorecards.
    public struct ScorecardList: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// List of QaScorecardRevisions.
      public var qaScorecardRevisions: [Swift.String] = []

      /// Initialize a new instance of `ScorecardList`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ScorecardList().with { $0.qaScorecardRevisions = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.contactcenterinsights.v1.AnnotatorSelector.QaConfig.ScorecardList"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Which scorecards should be scored.
    public enum OneOf_ScorecardSource: Codable, Equatable, Sendable {
      /// A manual list of scorecards to score.
      indirect case scorecardList(AnnotatorSelector.QaConfig.ScorecardList?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.contactcenterinsights.v1.AnnotatorSelector.QaConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.AnnotatorSelector"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
