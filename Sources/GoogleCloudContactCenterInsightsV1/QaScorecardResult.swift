// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// The results of scoring a single conversation against a QaScorecard. Contains
/// a collection of QaAnswers and aggregate score.
public struct QaScorecardResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Identifier. The name of the scorecard result.
  /// Format:
  /// projects/{project}/locations/{location}/qaScorecardResults/{qa_scorecard_result}
  public var name: Swift.String = Swift.String()

  /// The QaScorecardRevision scored by this result.
  public var qaScorecardRevision: Swift.String = Swift.String()

  /// The conversation scored by this result.
  public var conversation: Swift.String = Swift.String()

  /// Output only. The timestamp that the revision was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// ID of the agent that handled the conversation.
  public var agentId: Swift.String = Swift.String()

  /// Set of QaAnswers represented in the result.
  public var qaAnswers: [QaAnswer] = []

  /// The overall numerical score of the result, incorporating any manual edits
  /// if they exist.
  public var score: Swift.Double? = nil

  /// The maximum potential overall score of the scorecard. Any questions
  /// answered using `na_value` are excluded from this calculation.
  public var potentialScore: Swift.Double? = nil

  /// The normalized score, which is the score divided by the potential score.
  /// Any manual edits are included if they exist.
  public var normalizedScore: Swift.Double? = nil

  /// Collection of tags and their scores.
  public var qaTagResults: [QaScorecardResult.QaTagResult] = []

  /// List of all individual score sets.
  public var scoreSources: [QaScorecardResult.ScoreSource] = []

  /// Initialize a new instance of `QaScorecardResult`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = QaScorecardResult().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Tags and their corresponding results.
  public struct QaTagResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The tag the score applies to.
    public var tag: Swift.String = Swift.String()

    /// The score the tag applies to.
    public var score: Swift.Double? = nil

    /// The potential score the tag applies to.
    public var potentialScore: Swift.Double? = nil

    /// The normalized score the tag applies to.
    public var normalizedScore: Swift.Double? = nil

    /// Initialize a new instance of `QaTagResult`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = QaTagResult().with { $0.tag = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.QaScorecardResult.QaTagResult"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// A scorecard result may have multiple sets of scores from varying sources,
  /// one of which becomes the "main" answer above. A ScoreSource represents
  /// each individual set of scores.
  public struct ScoreSource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// What created the score.
    public var sourceType: QaScorecardResult.ScoreSource.SourceType = QaScorecardResult.ScoreSource
      .SourceType()

    /// The overall numerical score of the result.
    public var score: Swift.Double? = nil

    /// The maximum potential overall score of the scorecard. Any questions
    /// answered using `na_value` are excluded from this calculation.
    public var potentialScore: Swift.Double? = nil

    /// The normalized score, which is the score divided by the potential score.
    public var normalizedScore: Swift.Double? = nil

    /// Collection of tags and their scores.
    public var qaTagResults: [QaScorecardResult.QaTagResult] = []

    /// Initialize a new instance of `ScoreSource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ScoreSource().with { $0.sourceType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// What created the score.
    public enum SourceType: Codable, Equatable, Sendable {
      /// Source type is unspecified.
      case unspecified
      /// Score is derived only from system-generated answers.
      case systemGeneratedOnly
      /// Score is derived from both system-generated answers, and includes
      /// any manual edits if they exist.
      case includesManualEdits
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .systemGeneratedOnly: return 1
        case .includesManualEdits: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SOURCE_TYPE_UNSPECIFIED"
        case .systemGeneratedOnly: return "SYSTEM_GENERATED_ONLY"
        case .includesManualEdits: return "INCLUDES_MANUAL_EDITS"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SourceType/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SOURCE_TYPE_UNSPECIFIED": self = .unspecified
        case "SYSTEM_GENERATED_ONLY": self = .systemGeneratedOnly
        case "INCLUDES_MANUAL_EDITS": self = .includesManualEdits
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SourceType/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .systemGeneratedOnly
        case 2: self = .includesManualEdits
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .systemGeneratedOnly: return try container.encode(1)
        case .includesManualEdits: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.QaScorecardResult.ScoreSource"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.QaScorecardResult"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
