// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// The call participant speaking for a given utterance.
public struct ConversationParticipant: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Deprecated. Use `dialogflow_participant_name` instead.
  /// The name of the Dialogflow participant. Format:
  /// projects/{project}/locations/{location}/conversations/{conversation}/participants/{participant}
  @available(*, deprecated)
  public var dialogflowParticipant: Swift.String = Swift.String()

  /// Obfuscated user ID from Dialogflow.
  public var obfuscatedExternalUserId: Swift.String = Swift.String()

  /// The role of the participant.
  public var role: ConversationParticipant.Role = ConversationParticipant.Role()

  public var participant: OneOf_Participant? = nil

  /// Initialize a new instance of `ConversationParticipant`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ConversationParticipant().with { $0.dialogflowParticipantName = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case dialogflowParticipantName = "dialogflowParticipantName"
    case userId = "userId"
    case dialogflowParticipant = "dialogflowParticipant"
    case obfuscatedExternalUserId = "obfuscatedExternalUserId"
    case role = "role"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.dialogflowParticipant = try container.decode(
      Swift.String.self, forKey: .dialogflowParticipant)
    self.obfuscatedExternalUserId = try container.decode(
      Swift.String.self, forKey: .obfuscatedExternalUserId)
    self.role = try container.decode(ConversationParticipant.Role.self, forKey: .role)

    var participant: OneOf_Participant? = nil
    let participantCheckAndSet = {
      if participant != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'participant'"))
      }
      participant = $0
    }
    if let dialogflowParticipantName = try container.decodeIfPresent(
      Swift.String.self, forKey: .dialogflowParticipantName)
    {
      try participantCheckAndSet(.dialogflowParticipantName(dialogflowParticipantName))
    }
    if let userId = try container.decodeIfPresent(Swift.String.self, forKey: .userId) {
      try participantCheckAndSet(.userId(userId))
    }
    self.participant = participant
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.dialogflowParticipant, forKey: .dialogflowParticipant)
    try container.encode(self.obfuscatedExternalUserId, forKey: .obfuscatedExternalUserId)
    try container.encode(self.role, forKey: .role)

    if let choice = self.participant {
      switch choice {
      case .dialogflowParticipantName(let value):
        try container.encode(value, forKey: .dialogflowParticipantName)
      case .userId(let value):
        try container.encode(value, forKey: .userId)
      }
    }
  }

  /// The role of the participant.
  public enum Role: Codable, Equatable, Sendable {
    /// Participant's role is not set.
    case unspecified
    /// Participant is a human agent.
    case humanAgent
    /// Participant is an automated agent.
    case automatedAgent
    /// Participant is an end user who conversed with the contact center.
    case endUser
    /// Participant is either a human or automated agent.
    case anyAgent
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .humanAgent: return 1
      case .automatedAgent: return 2
      case .endUser: return 3
      case .anyAgent: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "ROLE_UNSPECIFIED"
      case .humanAgent: return "HUMAN_AGENT"
      case .automatedAgent: return "AUTOMATED_AGENT"
      case .endUser: return "END_USER"
      case .anyAgent: return "ANY_AGENT"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Role/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "ROLE_UNSPECIFIED": self = .unspecified
      case "HUMAN_AGENT": self = .humanAgent
      case "AUTOMATED_AGENT": self = .automatedAgent
      case "END_USER": self = .endUser
      case "ANY_AGENT": self = .anyAgent
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Role/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .humanAgent
      case 2: self = .automatedAgent
      case 3: self = .endUser
      case 4: self = .anyAgent
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .humanAgent: return try container.encode(1)
      case .automatedAgent: return try container.encode(2)
      case .endUser: return try container.encode(3)
      case .anyAgent: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public enum OneOf_Participant: Codable, Equatable, Sendable {
    /// The name of the participant provided by Dialogflow. Format:
    /// projects/{project}/locations/{location}/conversations/{conversation}/participants/{participant}
    case dialogflowParticipantName(Swift.String)
    /// A user-specified ID representing the participant.
    case userId(Swift.String)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.ConversationParticipant"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
