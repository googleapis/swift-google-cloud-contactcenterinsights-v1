// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt
import GoogleType

/// The response for querying metrics.
public struct QueryMetricsResponse: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. The location of the data.
  /// "projects/{project}/locations/{location}"
  public var location: Swift.String = Swift.String()

  /// The metrics last update time.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// A slice contains a total and (if the request specified a time granularity)
  /// a time series of metric values. Each slice contains a unique combination of
  /// the cardinality of dimensions from the request.
  public var slices: [QueryMetricsResponse.Slice] = []

  /// The macro average slice contains aggregated averages across the selected
  /// dimension. i.e. if group_by agent is specified this field will contain the
  /// average across all agents.
  /// This field is only populated if the request specifies a Dimension.
  public var macroAverageSlice: QueryMetricsResponse.Slice? = nil

  /// Initialize a new instance of `QueryMetricsResponse`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = QueryMetricsResponse().with { $0.location = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// A slice contains a total and (if the request specified a time granularity)
  /// a time series of metric values. Each slice contains a unique combination of
  /// the cardinality of dimensions from the request.
  ///
  /// For example, if the request specifies a single ISSUE dimension and it has a
  /// cardinality of 2 (i.e. the data used to compute the metrics has 2 issues in
  /// total), the response will have 2 slices:
  ///
  /// * Slice 1 -> dimensions=[Issue 1]
  /// * Slice 2 -> dimensions=[Issue 2]
  public struct Slice: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// A unique combination of dimensions that this slice represents.
    public var dimensions: [Dimension] = []

    /// The total metric value. The interval of this data point is
    /// [starting create time, ending create time) from the request.
    public var total: QueryMetricsResponse.Slice.DataPoint? = nil

    /// A time series of metric values. This is only populated if the request
    /// specifies a time granularity other than NONE.
    public var timeSeries: QueryMetricsResponse.Slice.TimeSeries? = nil

    /// Initialize a new instance of `Slice`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Slice().with { $0.dimensions = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// A data point contains the metric values mapped to an interval.
    public struct DataPoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The interval that this data point represents.
      ///
      /// * If this is the total data point, the interval is
      /// [starting create time, ending create time) from the request.
      /// * If this a data point from the time series, the interval is
      /// [time, time + time granularity from the request).
      public var interval: GoogleType.Interval? = nil

      /// The measure included in this data point.
      public var measure: OneOf_Measure? = nil

      /// Initialize a new instance of `DataPoint`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = DataPoint().with { $0.conversationMeasure = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case conversationMeasure = "conversationMeasure"
        case interval = "interval"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.interval = try container.decodeIfPresent(GoogleType.Interval.self, forKey: .interval)

        var measure: OneOf_Measure? = nil
        let measureCheckAndSet = {
          if measure != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'measure'"))
          }
          measure = $0
        }
        if let conversationMeasure = try container.decodeIfPresent(
          QueryMetricsResponse.Slice.DataPoint.ConversationMeasure?.self,
          forKey: .conversationMeasure)
        {
          try measureCheckAndSet(.conversationMeasure(conversationMeasure))
        }
        self.measure = measure
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.interval, forKey: .interval)

        if let choice = self.measure {
          switch choice {
          case .conversationMeasure(let value):
            try container.encode(value, forKey: .conversationMeasure)
          }
        }
      }

      /// The measure related to conversations.
      public struct ConversationMeasure: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// The conversation count.
        public var conversationCount: Swift.Int32? = nil

        /// The average silence percentage.
        public var averageSilencePercentage: Swift.Float? = nil

        /// The average duration.
        public var averageDuration: GoogleCloudWkt.Duration? = nil

        /// The average turn count.
        public var averageTurnCount: Swift.Float? = nil

        /// The average agent's sentiment score.
        public var averageAgentSentimentScore: Swift.Float? = nil

        /// The average client's sentiment score.
        public var averageClientSentimentScore: Swift.Float? = nil

        /// The average customer satisfaction rating.
        public var averageCustomerSatisfactionRating: Swift.Double? = nil

        /// Average QA normalized score.
        /// Will exclude 0's in average calculation.
        public var averageQaNormalizedScore: Swift.Double? = nil

        /// Average QA normalized score for all the tags.
        public var qaTagScores:
          [QueryMetricsResponse.Slice.DataPoint.ConversationMeasure.QaTagScore] = []

        /// Average QA normalized score averaged for questions averaged across
        /// all revisions of the parent scorecard.
        /// Will be only populated if the request specifies a dimension of
        /// QA_QUESTION_ID.
        public var averageQaQuestionNormalizedScore: Swift.Double? = nil

        /// Initialize a new instance of `ConversationMeasure`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ConversationMeasure().with { $0.conversationCount = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Average QA normalized score for the tag.
        public struct QaTagScore: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Tag name.
          public var tag: Swift.String = Swift.String()

          /// Average tag normalized score per tag.
          public var averageTagNormalizedScore: Swift.Double = Swift.Double()

          /// Initialize a new instance of `QaTagScore`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = QaTagScore().with { $0.tag = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse.Slice.DataPoint.ConversationMeasure.QaTagScore"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse.Slice.DataPoint.ConversationMeasure"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The measure included in this data point.
      public enum OneOf_Measure: Codable, Equatable, Sendable {
        /// The measure related to conversations.
        indirect case conversationMeasure(QueryMetricsResponse.Slice.DataPoint.ConversationMeasure?)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse.Slice.DataPoint"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// A time series of metric values.
    public struct TimeSeries: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The data points that make up the time series .
      public var dataPoints: [QueryMetricsResponse.Slice.DataPoint] = []

      /// Initialize a new instance of `TimeSeries`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TimeSeries().with { $0.dataPoints = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse.Slice.TimeSeries"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse.Slice"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.QueryMetricsResponse"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
