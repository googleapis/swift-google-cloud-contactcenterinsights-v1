// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Request for the BulkDownloadFeedbackLabel endpoint.
public struct BulkDownloadFeedbackLabelsRequest: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Required. The parent resource for new feedback labels.
  public var parent: Swift.String = Swift.String()

  /// Optional. A filter to reduce results to a specific subset. Supports
  /// disjunctions (OR) and conjunctions (AND).
  ///
  /// Supported fields:
  ///
  /// * `issue_model_id`
  /// * `qa_question_id`
  /// * `qa_scorecard_id`
  /// * `min_create_time`
  /// * `max_create_time`
  /// * `min_update_time`
  /// * `max_update_time`
  /// * `feedback_label_type`: QUALITY_AI, TOPIC_MODELING
  public var filter: Swift.String = Swift.String()

  /// Optional. Limits the maximum number of feedback labels that will be
  /// downloaded. The first `N` feedback labels will be downloaded.
  public var maxDownloadCount: Swift.Int32 = Swift.Int32()

  /// Optional. The type of feedback labels that will be downloaded.
  public var feedbackLabelType: BulkDownloadFeedbackLabelsRequest.FeedbackLabelType =
    BulkDownloadFeedbackLabelsRequest.FeedbackLabelType()

  /// Optional. Filter parent conversations to download feedback labels for.
  /// When specified, the feedback labels will be downloaded for the
  /// conversations that match the filter.
  /// If `template_qa_scorecard_id` is set, all the conversations that match the
  /// filter will be paired with the questions under the scorecard for labeling.
  public var conversationFilter: Swift.String = Swift.String()

  /// Optional. If set, a template for labeling conversations and scorecard
  /// questions will be created from the conversation_filter and the questions
  /// under the scorecard(s). The feedback label `filter` will be ignored.
  public var templateQaScorecardId: [Swift.String] = []

  /// Configuration for an external data store to which the feedback labels
  /// will be written to.
  public var destination: OneOf_Destination? = nil

  /// Initialize a new instance of `BulkDownloadFeedbackLabelsRequest`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = BulkDownloadFeedbackLabelsRequest().with { $0.gcsDestination = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case gcsDestination = "gcsDestination"
    case parent = "parent"
    case filter = "filter"
    case maxDownloadCount = "maxDownloadCount"
    case feedbackLabelType = "feedbackLabelType"
    case conversationFilter = "conversationFilter"
    case templateQaScorecardId = "templateQaScorecardId"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.parent = try container.decode(Swift.String.self, forKey: .parent)
    self.filter = try container.decode(Swift.String.self, forKey: .filter)
    self.maxDownloadCount = try container.decode(Swift.Int32.self, forKey: .maxDownloadCount)
    self.feedbackLabelType = try container.decode(
      BulkDownloadFeedbackLabelsRequest.FeedbackLabelType.self, forKey: .feedbackLabelType)
    self.conversationFilter = try container.decode(Swift.String.self, forKey: .conversationFilter)
    self.templateQaScorecardId = try container.decode(
      [Swift.String].self, forKey: .templateQaScorecardId)

    var destination: OneOf_Destination? = nil
    let destinationCheckAndSet = {
      if destination != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'destination'"))
      }
      destination = $0
    }
    if let gcsDestination = try container.decodeIfPresent(
      BulkDownloadFeedbackLabelsRequest.GcsDestination?.self, forKey: .gcsDestination)
    {
      try destinationCheckAndSet(.gcsDestination(gcsDestination))
    }
    self.destination = destination
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.parent, forKey: .parent)
    try container.encode(self.filter, forKey: .filter)
    try container.encode(self.maxDownloadCount, forKey: .maxDownloadCount)
    try container.encode(self.feedbackLabelType, forKey: .feedbackLabelType)
    try container.encode(self.conversationFilter, forKey: .conversationFilter)
    try container.encode(self.templateQaScorecardId, forKey: .templateQaScorecardId)

    if let choice = self.destination {
      switch choice {
      case .gcsDestination(let value):
        try container.encode(value, forKey: .gcsDestination)
      }
    }
  }

  /// Google Cloud Storage Object details to write the feedback labels to.
  public struct GcsDestination: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. File format in which the labels will be exported.
    public var format: BulkDownloadFeedbackLabelsRequest.GcsDestination.Format =
      BulkDownloadFeedbackLabelsRequest.GcsDestination.Format()

    /// Required. The Google Cloud Storage URI to write the feedback labels to.
    /// The file name will be used as a prefix for the files written to the
    /// bucket if the output needs to be split across multiple files, otherwise
    /// it will be used as is. The file extension will be appended to the file
    /// name based on the format selected.
    ///  E.g. `gs://bucket_name/object_uri_prefix`
    public var objectUri: Swift.String = Swift.String()

    /// Optional. Add whitespace to the JSON file. Makes easier to read, but
    /// increases file size. Only applicable for JSON format.
    public var addWhitespace: Swift.Bool = Swift.Bool()

    /// Optional. Always print fields with no presence.
    /// This is useful for printing fields that are not set, like implicit 0
    /// value or empty lists/maps. Only applicable for JSON format.
    public var alwaysPrintEmptyFields: Swift.Bool = Swift.Bool()

    /// Optional. The number of records per file. Applicable for either format.
    public var recordsPerFileCount: Swift.Int64 = Swift.Int64()

    /// Initialize a new instance of `GcsDestination`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GcsDestination().with { $0.format = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// All permissible file formats.
    /// See `records_per_file_count` to override the default number of records
    /// per file.
    public enum Format: Codable, Equatable, Sendable {
      /// Unspecified format.
      case unspecified
      /// CSV format.
      /// 1,000 labels are stored per CSV file by default.
      case csv
      /// JSON format.
      /// 1 label stored per JSON file by default.
      case json
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .csv: return 1
        case .json: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "FORMAT_UNSPECIFIED"
        case .csv: return "CSV"
        case .json: return "JSON"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Format/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "FORMAT_UNSPECIFIED": self = .unspecified
        case "CSV": self = .csv
        case "JSON": self = .json
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Format/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .csv
        case 2: self = .json
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .csv: return try container.encode(1)
        case .json: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.BulkDownloadFeedbackLabelsRequest.GcsDestination"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Possible feedback label types that will be downloaded.
  public enum FeedbackLabelType: Codable, Equatable, Sendable {
    /// Unspecified format
    case unspecified
    /// Downloaded file will contain all Quality AI labels from the latest
    /// scorecard revision.
    case qualityAi
    /// Downloaded file will contain only Topic Modeling labels.
    case topicModeling
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .qualityAi: return 1
      case .topicModeling: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "FEEDBACK_LABEL_TYPE_UNSPECIFIED"
      case .qualityAi: return "QUALITY_AI"
      case .topicModeling: return "TOPIC_MODELING"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:FeedbackLabelType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "FEEDBACK_LABEL_TYPE_UNSPECIFIED": self = .unspecified
      case "QUALITY_AI": self = .qualityAi
      case "TOPIC_MODELING": self = .topicModeling
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:FeedbackLabelType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .qualityAi
      case 2: self = .topicModeling
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .qualityAi: return try container.encode(1)
      case .topicModeling: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Configuration for an external data store to which the feedback labels
  /// will be written to.
  public enum OneOf_Destination: Codable, Equatable, Sendable {
    /// A cloud storage bucket destination.
    indirect case gcsDestination(BulkDownloadFeedbackLabelsRequest.GcsDestination?)
  }

  public static var _anyTypeUrl: Swift.String {
    return
      "type.googleapis.com/google.cloud.contactcenterinsights.v1.BulkDownloadFeedbackLabelsRequest"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
