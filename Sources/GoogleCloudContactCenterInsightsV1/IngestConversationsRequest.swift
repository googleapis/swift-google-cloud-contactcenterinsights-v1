// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// The request to ingest conversations.
public struct IngestConversationsRequest: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Required. The parent resource for new conversations.
  public var parent: Swift.String = Swift.String()

  /// Configuration that applies to all conversations.
  public var conversationConfig: IngestConversationsRequest.ConversationConfig? = nil

  /// Optional. DLP settings for transcript redaction. Optional, will default to
  /// the config specified in Settings.
  public var redactionConfig: RedactionConfig? = nil

  /// Optional. Default Speech-to-Text configuration. Optional, will default to
  /// the config specified in Settings.
  public var speechConfig: SpeechConfig? = nil

  /// Optional. If set, this fields indicates the number of objects to ingest
  /// from the Cloud Storage bucket. If empty, the entire bucket will be
  /// ingested. Unless they are first deleted, conversations produced through
  /// sampling won't be ingested by subsequent ingest requests.
  public var sampleSize: Swift.Int32? = nil

  /// Configuration for an external data store containing objects that will
  /// be converted to conversations.
  public var source: OneOf_Source? = nil

  /// Configuration for converting individual `source` objects to conversations.
  public var objectConfig: OneOf_ObjectConfig? = nil

  /// Initialize a new instance of `IngestConversationsRequest`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = IngestConversationsRequest().with { $0.gcsSource = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case gcsSource = "gcsSource"
    case transcriptObjectConfig = "transcriptObjectConfig"
    case parent = "parent"
    case conversationConfig = "conversationConfig"
    case redactionConfig = "redactionConfig"
    case speechConfig = "speechConfig"
    case sampleSize = "sampleSize"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.parent = try container.decode(Swift.String.self, forKey: .parent)
    self.conversationConfig = try container.decodeIfPresent(
      IngestConversationsRequest.ConversationConfig.self, forKey: .conversationConfig)
    self.redactionConfig = try container.decodeIfPresent(
      RedactionConfig.self, forKey: .redactionConfig)
    self.speechConfig = try container.decodeIfPresent(SpeechConfig.self, forKey: .speechConfig)
    self.sampleSize = try container.decodeIfPresent(Swift.Int32.self, forKey: .sampleSize)

    var source: OneOf_Source? = nil
    let sourceCheckAndSet = {
      if source != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'source'"))
      }
      source = $0
    }
    if let gcsSource = try container.decodeIfPresent(
      IngestConversationsRequest.GcsSource?.self, forKey: .gcsSource)
    {
      try sourceCheckAndSet(.gcsSource(gcsSource))
    }
    self.source = source

    var objectConfig: OneOf_ObjectConfig? = nil
    let objectConfigCheckAndSet = {
      if objectConfig != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'objectConfig'"))
      }
      objectConfig = $0
    }
    if let transcriptObjectConfig = try container.decodeIfPresent(
      IngestConversationsRequest.TranscriptObjectConfig?.self, forKey: .transcriptObjectConfig)
    {
      try objectConfigCheckAndSet(.transcriptObjectConfig(transcriptObjectConfig))
    }
    self.objectConfig = objectConfig
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.parent, forKey: .parent)
    try container.encode(self.conversationConfig, forKey: .conversationConfig)
    try container.encode(self.redactionConfig, forKey: .redactionConfig)
    try container.encode(self.speechConfig, forKey: .speechConfig)
    try container.encode(self.sampleSize, forKey: .sampleSize)

    if let choice = self.source {
      switch choice {
      case .gcsSource(let value):
        try container.encode(value, forKey: .gcsSource)
      }
    }

    if let choice = self.objectConfig {
      switch choice {
      case .transcriptObjectConfig(let value):
        try container.encode(value, forKey: .transcriptObjectConfig)
      }
    }
  }

  /// Configuration for Cloud Storage bucket sources.
  public struct GcsSource: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. The Cloud Storage bucket containing source objects.
    public var bucketUri: Swift.String = Swift.String()

    /// Optional. Specifies the type of the objects in `bucket_uri`.
    public var bucketObjectType: IngestConversationsRequest.GcsSource.BucketObjectType =
      IngestConversationsRequest.GcsSource.BucketObjectType()

    /// Optional. The Cloud Storage path to the conversation metadata. Note that:
    /// [1] Metadata files are expected to be in JSON format.
    /// [2] Metadata and source files (transcripts or audio) must be in
    ///     separate buckets.
    /// [3] A source file and its corresponding metadata file must share the same
    /// name to
    ///     be properly ingested, E.g. `gs://bucket/audio/conversation1.mp3` and
    ///     `gs://bucket/metadata/conversation1.json`.
    public var metadataBucketUri: Swift.String? = nil

    /// Optional. Custom keys to extract as conversation labels from metadata
    /// files in `metadata_bucket_uri`. Keys not included in this field will be
    /// ignored. Note that there is a limit of 100 labels per conversation.
    public var customMetadataKeys: [Swift.String] = []

    /// Initialize a new instance of `GcsSource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GcsSource().with { $0.bucketUri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public enum BucketObjectType: Codable, Equatable, Sendable {
      /// The object type is unspecified and will default to `TRANSCRIPT`.
      case unspecified
      /// The object is a transcript.
      case transcript
      /// The object is an audio file.
      case audio
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .transcript: return 1
        case .audio: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "BUCKET_OBJECT_TYPE_UNSPECIFIED"
        case .transcript: return "TRANSCRIPT"
        case .audio: return "AUDIO"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:BucketObjectType/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "BUCKET_OBJECT_TYPE_UNSPECIFIED": self = .unspecified
        case "TRANSCRIPT": self = .transcript
        case "AUDIO": self = .audio
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:BucketObjectType/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .transcript
        case 2: self = .audio
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .transcript: return try container.encode(1)
        case .audio: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.IngestConversationsRequest.GcsSource"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration for processing transcript objects.
  public struct TranscriptObjectConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. The medium transcript objects represent.
    public var medium: Conversation.Medium = Conversation.Medium()

    /// Initialize a new instance of `TranscriptObjectConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TranscriptObjectConfig().with { $0.medium = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.IngestConversationsRequest.TranscriptObjectConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration that applies to all conversations.
  public struct ConversationConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. An opaque, user-specified string representing a human agent who
    /// handled all conversations in the import. Note that this will be
    /// overridden if per-conversation metadata is provided through the
    /// `metadata_bucket_uri`.
    public var agentId: Swift.String = Swift.String()

    /// Optional. Indicates which of the channels, 1 or 2, contains the agent.
    /// Note that this must be set for conversations to be properly displayed and
    /// analyzed.
    public var agentChannel: Swift.Int32 = Swift.Int32()

    /// Optional. Indicates which of the channels, 1 or 2, contains the agent.
    /// Note that this must be set for conversations to be properly displayed and
    /// analyzed.
    public var customerChannel: Swift.Int32 = Swift.Int32()

    /// Initialize a new instance of `ConversationConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ConversationConfig().with { $0.agentId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.IngestConversationsRequest.ConversationConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Configuration for an external data store containing objects that will
  /// be converted to conversations.
  public enum OneOf_Source: Codable, Equatable, Sendable {
    /// A cloud storage bucket source. Note that any previously ingested objects
    /// from the source will be skipped to avoid duplication.
    indirect case gcsSource(IngestConversationsRequest.GcsSource?)
  }

  /// Configuration for converting individual `source` objects to conversations.
  public enum OneOf_ObjectConfig: Codable, Equatable, Sendable {
    /// Configuration for when `source` contains conversation transcripts.
    indirect case transcriptObjectConfig(IngestConversationsRequest.TranscriptObjectConfig?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.IngestConversationsRequest"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
