// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// The conversation resource.
public struct Conversation: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Immutable. The resource name of the conversation.
  /// Format:
  /// projects/{project}/locations/{location}/conversations/{conversation}
  public var name: Swift.String = Swift.String()

  /// The source of the audio and transcription for the conversation.
  public var dataSource: ConversationDataSource? = nil

  /// Output only. The time at which the conversation was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The most recent time at which the conversation was updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// The time at which the conversation started.
  public var startTime: GoogleCloudWKT.Timestamp? = nil

  /// A user-specified language code for the conversation.
  public var languageCode: Swift.String = Swift.String()

  /// An opaque, user-specified string representing the human agent who handled
  /// the conversation.
  public var agentId: Swift.String = Swift.String()

  /// A map for the user to specify any custom fields. A maximum of 100 labels
  /// per conversation is allowed, with a maximum of 256 characters per entry.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Conversation metadata related to quality management.
  public var qualityMetadata: Conversation.QualityMetadata? = nil

  /// Input only. JSON metadata encoded as a string.
  /// This field is primarily used by Insights integrations with various telphony
  /// systems and must be in one of Insight's supported formats.
  public var metadataJson: Swift.String = Swift.String()

  /// Output only. The conversation transcript.
  public var transcript: Conversation.Transcript? = nil

  /// Immutable. The conversation medium, if unspecified will default to
  /// PHONE_CALL.
  public var medium: Conversation.Medium = Conversation.Medium()

  /// Output only. The duration of the conversation.
  public var duration: GoogleCloudWKT.Duration? = nil

  /// Output only. The number of turns in the conversation.
  public var turnCount: Swift.Int32 = Swift.Int32()

  /// Output only. The conversation's latest analysis, if one exists.
  public var latestAnalysis: Analysis? = nil

  /// Output only. Latest summary of the conversation.
  public var latestSummary: ConversationSummarizationSuggestionData? = nil

  /// Output only. The annotations that were generated during the customer and
  /// agent interaction.
  public var runtimeAnnotations: [RuntimeAnnotation] = []

  /// Output only. All the matched Dialogflow intents in the call. The key
  /// corresponds to a Dialogflow intent, format:
  /// projects/{project}/agent/{agent}/intents/{intent}
  public var dialogflowIntents: [Swift.String: DialogflowIntent] = [:]

  /// Obfuscated user ID which the customer sent to us.
  public var obfuscatedUserId: Swift.String = Swift.String()

  /// Metadata that applies to the conversation.
  public var metadata: OneOf_Metadata? = nil

  /// A time to live expiration setting, can be either a specified timestamp or a
  /// duration from the time that the conversation creation request was received.
  /// Conversations with an expiration set will be removed up to 24 hours after
  /// the specified time.
  public var expiration: OneOf_Expiration? = nil

  /// Initialize a new instance of `Conversation`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Conversation().with { $0.callMetadata = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case callMetadata = "callMetadata"
    case expireTime = "expireTime"
    case ttl = "ttl"
    case name = "name"
    case dataSource = "dataSource"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case startTime = "startTime"
    case languageCode = "languageCode"
    case agentId = "agentId"
    case labels = "labels"
    case qualityMetadata = "qualityMetadata"
    case metadataJson = "metadataJson"
    case transcript = "transcript"
    case medium = "medium"
    case duration = "duration"
    case turnCount = "turnCount"
    case latestAnalysis = "latestAnalysis"
    case latestSummary = "latestSummary"
    case runtimeAnnotations = "runtimeAnnotations"
    case dialogflowIntents = "dialogflowIntents"
    case obfuscatedUserId = "obfuscatedUserId"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.dataSource = try container.decodeIfPresent(
      ConversationDataSource.self, forKey: .dataSource)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .updateTime)
    self.startTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .startTime)
    self.languageCode = try container.decode(Swift.String.self, forKey: .languageCode)
    self.agentId = try container.decode(Swift.String.self, forKey: .agentId)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.qualityMetadata = try container.decodeIfPresent(
      Conversation.QualityMetadata.self, forKey: .qualityMetadata)
    self.metadataJson = try container.decode(Swift.String.self, forKey: .metadataJson)
    self.transcript = try container.decodeIfPresent(
      Conversation.Transcript.self, forKey: .transcript)
    self.medium = try container.decode(Conversation.Medium.self, forKey: .medium)
    self.duration = try container.decodeIfPresent(GoogleCloudWKT.Duration.self, forKey: .duration)
    self.turnCount = try container.decode(Swift.Int32.self, forKey: .turnCount)
    self.latestAnalysis = try container.decodeIfPresent(Analysis.self, forKey: .latestAnalysis)
    self.latestSummary = try container.decodeIfPresent(
      ConversationSummarizationSuggestionData.self, forKey: .latestSummary)
    self.runtimeAnnotations = try container.decode(
      [RuntimeAnnotation].self, forKey: .runtimeAnnotations)
    self.dialogflowIntents = try container.decode(
      [Swift.String: DialogflowIntent].self, forKey: .dialogflowIntents)
    self.obfuscatedUserId = try container.decode(Swift.String.self, forKey: .obfuscatedUserId)

    var metadata: OneOf_Metadata? = nil
    let metadataCheckAndSet = {
      if metadata != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'metadata'"))
      }
      metadata = $0
    }
    if let callMetadata = try container.decodeIfPresent(
      Conversation.CallMetadata?.self, forKey: .callMetadata)
    {
      try metadataCheckAndSet(.callMetadata(callMetadata))
    }
    self.metadata = metadata

    var expiration: OneOf_Expiration? = nil
    let expirationCheckAndSet = {
      if expiration != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'expiration'"))
      }
      expiration = $0
    }
    if let expireTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp?.self, forKey: .expireTime)
    {
      try expirationCheckAndSet(.expireTime(expireTime))
    }
    if let ttl = try container.decodeIfPresent(GoogleCloudWKT.Duration?.self, forKey: .ttl) {
      try expirationCheckAndSet(.ttl(ttl))
    }
    self.expiration = expiration
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.dataSource, forKey: .dataSource)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.startTime, forKey: .startTime)
    try container.encode(self.languageCode, forKey: .languageCode)
    try container.encode(self.agentId, forKey: .agentId)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.qualityMetadata, forKey: .qualityMetadata)
    try container.encode(self.metadataJson, forKey: .metadataJson)
    try container.encode(self.transcript, forKey: .transcript)
    try container.encode(self.medium, forKey: .medium)
    try container.encode(self.duration, forKey: .duration)
    try container.encode(self.turnCount, forKey: .turnCount)
    try container.encode(self.latestAnalysis, forKey: .latestAnalysis)
    try container.encode(self.latestSummary, forKey: .latestSummary)
    try container.encode(self.runtimeAnnotations, forKey: .runtimeAnnotations)
    try container.encode(self.dialogflowIntents, forKey: .dialogflowIntents)
    try container.encode(self.obfuscatedUserId, forKey: .obfuscatedUserId)

    if let choice = self.metadata {
      switch choice {
      case .callMetadata(let value):
        try container.encode(value, forKey: .callMetadata)
      }
    }

    if let choice = self.expiration {
      switch choice {
      case .expireTime(let value):
        try container.encode(value, forKey: .expireTime)
      case .ttl(let value):
        try container.encode(value, forKey: .ttl)
      }
    }
  }

  /// Call-specific metadata.
  public struct CallMetadata: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The audio channel that contains the customer.
    public var customerChannel: Swift.Int32 = Swift.Int32()

    /// The audio channel that contains the agent.
    public var agentChannel: Swift.Int32 = Swift.Int32()

    /// Initialize a new instance of `CallMetadata`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CallMetadata().with { $0.customerChannel = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.CallMetadata"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Conversation metadata related to quality management.
  public struct QualityMetadata: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// An arbitrary integer value indicating the customer's satisfaction rating.
    public var customerSatisfactionRating: Swift.Int32 = Swift.Int32()

    /// The amount of time the customer waited to connect with an agent.
    public var waitDuration: GoogleCloudWKT.Duration? = nil

    /// An arbitrary string value specifying the menu path the customer took.
    public var menuPath: Swift.String = Swift.String()

    /// Information about agents involved in the call.
    public var agentInfo: [Conversation.QualityMetadata.AgentInfo] = []

    /// Initialize a new instance of `QualityMetadata`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = QualityMetadata().with { $0.customerSatisfactionRating = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Information about an agent involved in the conversation.
    public struct AgentInfo: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// A user-specified string representing the agent.
      public var agentId: Swift.String = Swift.String()

      /// The agent's name.
      public var displayName: Swift.String = Swift.String()

      /// A user-specified string representing the agent's team.
      public var team: Swift.String = Swift.String()

      /// A user-provided string indicating the outcome of the agent's segment of
      /// the call.
      public var dispositionCode: Swift.String = Swift.String()

      /// The agent type, e.g. HUMAN_AGENT.
      public var agentType: ConversationParticipant.Role = ConversationParticipant.Role()

      /// Initialize a new instance of `AgentInfo`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = AgentInfo().with { $0.agentId = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.QualityMetadata.AgentInfo"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.QualityMetadata"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A message representing the transcript of a conversation.
  public struct Transcript: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// A list of sequential transcript segments that comprise the conversation.
    public var transcriptSegments: [Conversation.Transcript.TranscriptSegment] = []

    /// Initialize a new instance of `Transcript`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Transcript().with { $0.transcriptSegments = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// A segment of a full transcript.
    public struct TranscriptSegment: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// The time that the message occurred, if provided.
      public var messageTime: GoogleCloudWKT.Timestamp? = nil

      /// The text of this segment.
      public var text: Swift.String = Swift.String()

      /// A confidence estimate between 0.0 and 1.0 of the fidelity of this
      /// segment. A default value of 0.0 indicates that the value is unset.
      public var confidence: Swift.Float = Swift.Float()

      /// A list of the word-specific information for each word in the segment.
      public var words: [Conversation.Transcript.TranscriptSegment.WordInfo] = []

      /// The language code of this segment as a
      /// [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tag.
      /// Example: "en-US".
      public var languageCode: Swift.String = Swift.String()

      /// For conversations derived from multi-channel audio, this is the channel
      /// number corresponding to the audio from that channel. For
      /// audioChannelCount = N, its output values can range from '1' to 'N'. A
      /// channel tag of 0 indicates that the audio is mono.
      public var channelTag: Swift.Int32 = Swift.Int32()

      /// The participant of this segment.
      public var segmentParticipant: ConversationParticipant? = nil

      /// CCAI metadata relating to the current transcript segment.
      public var dialogflowSegmentMetadata:
        Conversation.Transcript.TranscriptSegment.DialogflowSegmentMetadata? = nil

      /// The sentiment for this transcript segment.
      public var sentiment: SentimentData? = nil

      /// Initialize a new instance of `TranscriptSegment`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TranscriptSegment().with { $0.messageTime = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Word-level info for words in a transcript.
      public struct WordInfo: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Time offset of the start of this word relative to the beginning of
        /// the total conversation.
        public var startOffset: GoogleCloudWKT.Duration? = nil

        /// Time offset of the end of this word relative to the beginning of the
        /// total conversation.
        public var endOffset: GoogleCloudWKT.Duration? = nil

        /// The word itself. Includes punctuation marks that surround the word.
        public var word: Swift.String = Swift.String()

        /// A confidence estimate between 0.0 and 1.0 of the fidelity of this
        /// word. A default value of 0.0 indicates that the value is unset.
        public var confidence: Swift.Float = Swift.Float()

        /// Initialize a new instance of `WordInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = WordInfo().with { $0.startOffset = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.Transcript.TranscriptSegment.WordInfo"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Metadata from Dialogflow relating to the current transcript segment.
      public struct DialogflowSegmentMetadata: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Whether the transcript segment was covered under the configured smart
        /// reply allowlist in Agent Assist.
        public var smartReplyAllowlistCovered: Swift.Bool = Swift.Bool()

        /// Initialize a new instance of `DialogflowSegmentMetadata`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = DialogflowSegmentMetadata().with { $0.smartReplyAllowlistCovered = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.Transcript.TranscriptSegment.DialogflowSegmentMetadata"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.Transcript.TranscriptSegment"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation.Transcript"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Possible media for the conversation.
  public enum Medium: Codable, Equatable, Sendable {
    /// Default value, if unspecified will default to PHONE_CALL.
    case unspecified
    /// The format for conversations that took place over the phone.
    case phoneCall
    /// The format for conversations that took place over chat.
    case chat
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .phoneCall: return 1
      case .chat: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "MEDIUM_UNSPECIFIED"
      case .phoneCall: return "PHONE_CALL"
      case .chat: return "CHAT"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Medium/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "MEDIUM_UNSPECIFIED": self = .unspecified
      case "PHONE_CALL": self = .phoneCall
      case "CHAT": self = .chat
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Medium/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .phoneCall
      case 2: self = .chat
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .phoneCall: return try container.encode(1)
      case .chat: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Metadata that applies to the conversation.
  public enum OneOf_Metadata: Codable, Equatable, Sendable {
    /// Call-specific metadata.
    indirect case callMetadata(Conversation.CallMetadata?)
  }

  /// A time to live expiration setting, can be either a specified timestamp or a
  /// duration from the time that the conversation creation request was received.
  /// Conversations with an expiration set will be removed up to 24 hours after
  /// the specified time.
  public enum OneOf_Expiration: Codable, Equatable, Sendable {
    /// The time at which this conversation should expire. After this time, the
    /// conversation data and any associated analyses will be deleted.
    indirect case expireTime(GoogleCloudWKT.Timestamp?)
    /// Input only. The TTL for this resource. If specified, then this TTL will
    /// be used to calculate the expire time.
    indirect case ttl(GoogleCloudWKT.Duration?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contactcenterinsights.v1.Conversation"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
